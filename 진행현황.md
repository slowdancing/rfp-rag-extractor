# 📊 프로젝트 진행 현황

> 입찰메이트 RFP RAG Extractor · 마지막 업데이트: **2026-07-03**

## 전체 진행도

```
████████████████████████  약 96%
모델확정·품질검증·자체호스팅 배포 + 비즈니스 기능(추천·요약·적격성). 발표/보고서 남음
```

---

## 📝 변경 이력 (수정사항 & 추가) — 최신순

> 새로 추가하거나 업데이트한 것을 여기에 날짜별로 요약해 적는다.

### 2026-07-03
- **적격성 판정 기능**(신규): `/eligibility` — RFP 입찰참가자격을 LLM으로 추출·회사 프로필과 대조 →
  항목별 O/X/? + verdict(적격/부적격/확인필요, status로 결정론적 산정). 프론트에 회사 프로필 폼 + 판정 표.
- **AI 요약을 고정 스키마 표로** — 10개 항목(사업명·발주기관·예산·기간·마감일·계약방식·자격·요구사항·평가·제출)
  JSON 생성 → 표 렌더링. 문서 간 동일 틀 비교 가능.
- **나라장터 실시간 폴백**(옵트인): `src/nara.py` + `/recommend` 관련성 게이트 →
  로컬에 적합 공고 없으면 조달청 API로 실시간 검색(키 있을 때). `NARA_*` 설정 추가.
- **UI 점수를 코사인 유사도(퍼센트)로** — RRF(1위도 ~0.03) 대신 dense 코사인을 표시(순위는 RRF 유지).
- **로컬↔VM 연결 실행 확립** — VM FastAPI(EXAONE, :8501, 방화벽 개방) + 로컬 React(`VITE_API`) → 실서비스 구동.
- **UX 개선(피드백 반영)**: 적격성 회사프로필 **자연어 입력**, 도메인 밖 질문엔 **"적합 공고 없음" 안내**(관련성 게이트).
- **AI요약 안정화**(EXAONE 형식이탈 대응): 프롬프트 JSON→'항목:값' 줄형식+few-shot, 온도 0.8→0.2,
  파서 강건화(대화체·번호 흡수), **핵심 4항목(사업명·기관·예산·마감)은 카탈로그 메타데이터로 확정 채움** → 긴 문서도 표 안정.
- **문서 대거 최신화**: README·presentation·ollama_setup·gcp_setup·architecture를 EXAONE 확정·배포 기준으로 정리.
- **배포 트러블슈팅**(상세 `VM_배포_학습정리.md`): git pull 충돌→`reset --hard`, 옛 uvicorn 미종료로 코드 미반영,
  프론트가 로컬(8000) 호출 문제→`VITE_API`로 VM 지정.

### 2026-07-02
- **내용형(서술형) 골든셋 28→59개 확장** (문서 원문 근거로 직접 작성 31개, 문서 19종, 중립 정답)
- **서술형 답변품질 LLM-judge 교차검증** (`compare_llms_judge.py`, 심판 EXAONE+gpt-5-mini)
  - 결과: **EXAONE 4.37 > gpt-5-mini 4.20**. 상대 심판 gpt조차 EXAONE(4.31)>자기(4.14) → 자기편향 아님
  - 종합: 추출정확도 동률 + 서술형 품질·속도(2.6배)·비용·보안 우위 → EXAONE 자체호스팅 확정 재확인
- **VM 배포 외부접속 실동작**: GCP 방화벽으로 API 포트(8501) 개방(내 IP 한정) + uvicorn `0.0.0.0`
  → **로컬 React 브라우저에서 VM의 EXAONE 서비스 호출 성공** (프론트 API 주소 env화)
- **문서 정합성 갱신**: README·presentation·ollama_setup·gcp_setup을 확정모델(EXAONE)·배포 반영으로 최신화

### 2026-07-01
- **LLM 정량 비교 완료 → EXAONE-3.5-7.8B 최종 확정** (VM/Ollama, 골든셋 30건)
  - 결과: exaone3.5:7.8b **0.533**(1위) > exaone3.5:2.4b 0.467 > gemma2:2b 0.367 > qwen2.5:3b 0.333
  - 인사이트: EXAONE는 경량·풀사이즈 모두 타 계열 압도 → 한국어 특화가 파라미터 수보다 결정적
  - VM 메모리 안전: 모델 1개씩 실행, `ollama stop`으로 회수 (경량 양자화 3종 → 7.8B 순)
- **오픈소스 vs OpenAI 직접 비교(동일 bge-m3 검색) + 응답시간 측정**
  - **정확도 동률**: EXAONE-3.5-7.8B 0.533 = gpt-5-mini 0.533 (검색 조건 동일하게 맞춤)
  - **응답속도: EXAONE 3.78s vs gpt-5-mini 9.81s(중앙값) → EXAONE 약 2.6배 빠름**
  - 결론: **정확도 대등 + 속도·비용(무료)·기밀보호 우위** → 자체호스팅 채택 완전 정당화
  - (참고) gpt-5-mini 풀스택(OpenAI 임베딩)은 0.500 → bge-m3에서 0.533 상승 = 임베딩 bge-m3 우수성 재확인
- **compare_llms.py 고도화**: 결과 누적(`compare_llms.json` 원장) + `openai:` 접두사(OpenAI 클라우드 비교) + 응답시간(평균·중앙) 측정
- **실험 결과 results/로 분배**: `model_decision.md`(신규, 채택·의사결정·LLM인사이트),
  `eval_generation.md` 보강(내용형 28건 LLM-judge). 발표자료는 요약·인용만.
- **`.env.ollama.example` 기본 LLM = exaone3.5:7.8b** 로 변경 (배포 기본값)
- **VM 배포(자체호스팅 서비스화) 완료** — GCP VM에서 EXAONE RAG를 상시 서비스로 구동
  - Ollama 상시구동(EXAONE+bge-m3) + FastAPI 상시구동(`127.0.0.1:8500`, `/ask`·`/recommend` 동작)
  - 웹전용·무sudo·JupyterHub(8000 점유) 제약 → 방화벽/SSH터널 대신 **노트북 라이브 데모**로 시연
  - `notebooks/demo.ipynb` 추가: VM 커널이 localhost:8500(EXAONE API) 직접 호출(ipywidgets/urllib)
  - 트러블슈팅: 포트충돌(→8500), pip 권한(→설치회피), 노트북 localhost 혼동(→VM에서 실행),
    /recommend 500(→corpus_clean.csv 업로드). 상세: `VM_배포_학습정리.md`

### 2026-06-27
- **GUI 구현(FastAPI + React)**: 맞춤추천·필터목록·AI요약 화면 동작 확인
  - 백엔드: `src/catalog.py`, `/documents`·`/recommend` 엔드포인트 + CORS (`src/api`)
  - 프론트: `frontend/` (Vite React) — 고객역량 입력→추천, 필터, 온디맨드 AI요약
- **모델 확정**: 임베딩 **bge-m3**, LLM **EXAONE-3.5-7.8B**, 검색 **하이브리드**, 호스팅 자체(Ollama)
  - 근거: 임베딩 비교(bge-m3/KURE > OpenAI), Qwen 중국어 혼입 → EXAONE, 하이브리드 전지표 우위
- **평가** 내용형 골든셋 28건 생성평가(LLM-judge): 검색 Hit@5 0.96 / 답변 3.89/5
- **추가** 내용형 골든셋 직접 작성 18건(원문 대조, source=manual) → 총 28건
- **결과 저장**: `results/compare_embeddings.md`, `docs/presentation.md` 5절 확정 결과 반영
- (다음) GUI = FastAPI + React 로 결정 → 백엔드 API부터

### 2026-06-26
- **2단계 실행** GCP VM(L4 GPU)에서 Ollama로 자체호스팅 RAG 동작 (bge-m3 임베딩 + qwen2.5 LLM)
- **추가** OpenAI 호환 `base_url` 지원(`openai_base_url`) → Ollama를 기존 코드로 사용
- **추가** 임베딩 모델 비교 스크립트 `scripts/compare_embeddings.py`, 결과 `results/compare_embeddings.md`
- **추가** 내용형 골든셋 생성기 `scripts/make_content_goldenset.py`, 생성평가 `scripts/eval_generation.py`
- **추가** Ollama/GCP 가이드(`docs/ollama_setup.md`, `docs/gcp_setup.md`), 경량 의존성 `requirements-run.txt`
- **fix** loader pypdf 지연 import / Qwen 중국어 혼입 방지(프롬프트 한국어 강제)
- **문서** README 전면 최신화

### 2026-06-25
- **평가** 검색 평가(골든셋 285건): hybrid가 dense 대비 Hit@1 0.807→0.933, MRR 0.861→0.953 (`scripts/eval_retrieval.py`, `results/eval_retrieval.md`)
- **문서** 학습정리 Q32~34(인스턴스/클래스 속성, Hit@k·MRR, 컬렉션 분리) 추가, 진행현황 갱신

### 2026-06-24
- **추가** `src/dataset_utill` 청킹 PUA/대체문자(`U+F06D`, `U+FFFD`) 정제 (clean.py, chunker.py)
- **추가** 골든셋 초안 생성기 `scripts/make_goldenset.py` → `data/eval/eval_set.draft.jsonl` (285건)
- **수정** 청킹 단위를 `.env`(CHUNK_SIZE/OVERLAP)로 제어하도록 `chunk_corpus.main()` 연결
- **추가** 임베딩 모델별 **컬렉션 자동 분리** `vectorstore.collection_name()` (차원 충돌 방지)
- **검증** 로컬 HF 임베딩 경로(e5-small, CPU) — 코드 정상, 하이브리드 검색 1위 확인
- **추가** 의존성: `sentence-transformers`(로컬 HF), `rank_bm25`

### 2026-06-23
- **추가** 하이브리드 검색 `src/rag/hybrid.py` (BM25 문자 바이그램 + dense, RRF)
- **수정** gpt-5 계열 temperature 미지원 대응 (openai_llm.py)

### 2026-06-22
- **추가** 질의 재작성 `RAGPipeline.rewrite_query`
- **추가** 청크 크기 실험 `scripts/exp_chunk_size.py`, 결과 `results/retrieval_experiment.md`
- **리팩터** `src/ingestion` → `src/dataset_utill` 통합

---

## 단계별 현황

| # | 단계 | 상태 | 진행도 |
|---|------|------|--------|
| 1 | 프로젝트 셋업 (스캐폴드·git·브랜치) | ✅ 완료 | `██████████` 100% |
| 2 | 데이터 준비 (추출·보강·정제·청킹) | ✅ 완료 | `██████████` 100% |
| 3 | EDA | ✅ 완료 | `██████████` 100% |
| 4 | 임베딩 & 벡터 적재 (ChromaDB) | ✅ 완료 | `██████████` 100% |
| 5 | 질의응답 (검색→LLM→답변) | ✅ 동작 (질의재작성+하이브리드) | `█████████░` 90% |
| 6 | 검색 품질 개선 (하이브리드 검색) | ✅ 적용·검증 | `█████████░` 85% |
| 7 | 평가 체계 (지표·평가셋) | ✅ 검색·생성평가 + 서술형 LLM-judge 교차검증 | `██████████` 100% |
| 8 | 실험·튜닝 (청크/모델 비교) | ✅ 청크·임베딩·LLM·OpenAI대비·응답시간·답변품질 완료 | `██████████` 100% |
| 9 | GCP VM 2단계 (자체호스팅) | ✅ 배포 완료 (EXAONE API + 외부접속 실동작) | `██████████` 100% |
| 10 | GUI (FastAPI + React) | ✅ 추천·필터·AI요약 + VM EXAONE 연동 시연 | `██████████` 100% |
| 11 | 추가기능(추천 고도화) + 보고서/발표 | ⬜ 대기 | `░░░░░░░░░░` 0% |

범례: ✅ 완료 · 🟡 진행중/부분 · 🔴 막힘/조치필요 · ⬜ 미착수

---

## 파이프라인 진행 상태

```
[zip]
  │ extract_dataset()              ✅
  ▼
[data/raw + data/metadata]
  │ build_corpus()  (보강21건+정제)  ✅
  ▼
[corpus_clean.csv  (100행)]
  │ build_chunks()  (토큰청킹)       ✅
  ▼
[chunks.csv  (1,107 청크)]
  │ index_chunks()  (임베딩)         ✅  ← text-embedding-3-small
  ▼
[ChromaDB  (1,107 벡터)]
  │ rewrite_query() (질의 재작성)     ✅  ← 자연어 질문→키워드
  │ retrieve()  (하이브리드 BM25+dense) ✅  ← RRF 융합, 자연어 질문 해결
  ▼
[검색된 청크]
  │ ask()  (프롬프트→LLM)            ✅  ← gpt-5-mini, 환각억제 정상
  ▼
[답변 + 출처]
```

---

## ✅ 검색 품질 — 해결

**핵심 발견 (실험으로 검증)**: 검색 실패의 원인은 **청크 크기가 아니라 질의 표현**.
- 청크 256~1024 비교 → 모두 Hit@1 0.89~0.93, **1024 최적** (가설 반증)
- 자연어 질문은 흔한 일반어(사업·예산·기간)가 핵심어를 희석

**조치 (완료)**:
- [x] 질의 재작성(Query Rewriting) → 실패 질의 순위 20위 밖 → 12위
- [x] **하이브리드 검색(BM25 + dense, RRF)** → 실패 질의 **14위 밖 → 1위** ★
- [ ] 사업명 가중 인덱싱 / 재랭킹 (보조, 선택)

**검증**: end-to-end 재테스트에서 어제 실패 질문("~사업규모와 과업기간은?")이
이제 정확히 답변(87,000,000원 / 120일 + 출처). 상세: `results/retrieval_experiment.md`

---

## ✅ 완료한 핵심 산출물

- `data/processed/corpus_clean.csv` — 정제 코퍼스 100건 (짧은 문서 21건 원본 보강)
- `data/processed/chunks.csv` — 1,107 청크 (메타데이터 부착)
- `chroma_db/` — 1,107 벡터 (검색 가능)
- `src/dataset_utill/` — 데이터 파이프라인 (추출·정제·청킹·로더 통합)
- 질의 재작성 (`RAGPipeline.rewrite_query`)
- 하이브리드 검색 (`src/rag/hybrid.py`, BM25+dense RRF, 기본 모드)
- 청크 크기 실험 (`scripts/exp_chunk_size.py`, `results/retrieval_experiment.md`)
- GitHub `main` 커밋 `d2e89fa` 까지 반영

---

## 다음 마일스톤

```
[다음]  평가셋 제작 + 지표 선정 (Hit@k, MRR, LLM-judge)
   ↓
        모델/top_k 실험 → 최적 설정 확정
   ↓
        GCP VM(L4) + HuggingFace 전환 (2단계)
   ↓
        추가기능 + 보고서/발표
```
