폴더부터 설명을 시작할게요

우선 data 폴더 -> 이 폴더는 다운로드된 데이터셋으로부터 가공한 데이터가 들어있는 폴더인거고 왜 이렇게 해주냐 이 환경을 정확하게 맞추기 위함도 있고 (협업시 경로 문제)데이터를 쉽게 꺼내서 보기 위함도 있어 그니까 사실상 경로문제때문에 다운로드된 데이터셋의 경로를 맞춰주는거지 동일하고 알기쉽게

docs 폴더는 그냥 설명문서들이 있는 폴더

notebooks는 실험한 결과가 들어있는 폴더 아마 EDA파일 하나만 들어있음

results는 이제 결과값이 저장되어있는 폴더

scripts 폴더는 기본적으로 실행파일들이 들어있는 폴더인거야 터미널에서 실행을 하게 된다면 실제 저런 파일들을 실행하게 되는거

src폴더같은 경우에는 사실상 이 구조의 핵심 역할을 하는 파일들이 존재하는 폴더 -> 함수들이라던지 이런것들이 있는 폴더

tests폴더는 잘모르겠네 일단 뭐가 있긴함

자 이제 하위폴더를 설명하기 전에 다음 파일들부터 설명을 진행하자

.env파일 -> api키가 업로드되어있기 때문에 .gitignore에 추가가 되어있음, 기본적인 세팅값들이 저장되어있는 파일이라 나중에 후술할 세팅 파일에서 .env파일에 있는 값들을 불러와서 기본 세팅을 저장함

.gitattributes는 무슨 파일인지 잘 모르겠어? 이게 뭐지?

readme.md는 기본적으로 이 레포가 어떤 작업을 수행하고 어떤 구조로 짜여져있는지 들어있는 마크다운 파일

requirements들은 뭐지? 얘들은 아마 의존성이랑 작업환경을 기본적으로 세팅해주는 파일같은데 uv쪽은 아닌거같고 어디서 사용하는거지? 아무튼 잘 모르겠다

진행현황은.md는 말 그대로 진행현황을 저장한 마크다운 파일이고

이제 하위폴더들을 설명해보자면

우선 data의 하위폴더 eval -> 평가 데이터셋이 저장되어있는 폴더 == 골든셋을 여기에 저장해놓음 draft는 이제 문서가 어떤걸 찾아야하냐에 대한 답이고 content는 질문이 들어왔을 때 적절한 문장의 답이 어떤거냐를 저장해놓은 골든셋이다. 즉 eval 폴더는 골든셋이다 근데 이렇게 보니까 저 content쪽 예시가 부족하다는 생각이 들기는 하네 이걸 보완을 좀 해봐야겠다.

이제 metadata는 원본 데이터셋이 원본 파일 / 그걸 정리해놓은 csv / xlsx파일 즉 메타데이터로 나누어져 있었는데 그걸 저장하는 폴더 즉 원본데이터를 저장해놓는 폴더

processed는 전처리를 마친 데이터들이 저장되는 폴더 예를들자면 정제 + 보강을 마친 데이터는 corpus_clean에 저장 청킹을 마친 데이터는 chunks로 저장되는식 즉 실제로 사용할 데이터들이 임시저장되는 경로라고 바라보면 됨

raw는 원본 데이터셋에서 원본파일이 저장된 폴더

eval / meatadata , raw, processed 이렇게 나눌 수 있을듯? 성격에 따라서 실제로 데이터가 어떤 처리과정을 거쳤을 때 저장되는 폴더는 processed

docs폴더는 arhitecture는 이제 우리 작업이 어떻게 구성되어있는지 저장
setup들은 그냥 어떻게 설정을 해주냐에 대한 문서 << 굳이 넣을 필요는 없는 문서 라고 생각함>>

results폴더는 여러 결과값들이 저장되는 폴더인데 그냥 파일들 제목 그대로 그거에 대한 결과값들이 저장되는 폴더

scripts 폴더

__init__에는 아무것도 없음

ask.py는 실제로 우리가 실행하고 질문을 적는 파일

python -m ask.py "질문내용" << 이런식으로 질문을 할 수 있고>>

question에 질문을 집어넣고 pipeline으로 빌드한 모델을 호출해서 ask함수를 사용해 질문을 ans변수에 저장 그다음에 답변을 출력 ans변수에 sources로 분석

compare_embeddings.py는 